# The Interactive Neuron

Original Idea: A 3d model of a neuron where the user can interact with it to change it's position and viewpoint, zoom into the model for more detail or out for less detail, and click on specific parts of the neuron to learn about its functionality. This webpage will be used by psychology students to study and better understand the anatomy and functionality of individual neurons.

## Open as a Github Page
Open the following link in your preferred browser:
https://cntipping.github.io/Interactive_Neuron/

## Run locally

Requirements: Node.js 22.13+ and pnpm 11.

1. Download or clone this project.
2. Open a terminal in the folder containing `package.json`.
3. Install dependencies:

   ```sh
   pnpm install
   ```

4. Start the development server:

   ```sh
   pnpm dev
   ```

5. Open the `Local` address printed in the terminal, usually `http://localhost:3000`.

Keep the terminal open while using the site. Press `Ctrl+C` to stop it.

## Controls

- Drag to rotate the neuron.
- Point at a feature and scroll to zoom toward it.
- Use the structure index to pin a label.
- Hover for 0.6 seconds to preview a structure.
- Press Escape to close the active label.

## Useful commands

```sh
pnpm build
node --experimental-strip-types --test tests/*.test.mjs
```

The site is deployed at https://interactive-neuron-study.qs6bpnpyr8.chatgpt.site.

## GitHub Pages

Push this project to a GitHub repository’s `main` branch. The included GitHub Actions workflow builds and publishes the static site automatically. In the repository, open **Settings → Pages** and set **Source** to **GitHub Actions**. After the workflow finishes, the site will be available at:

```text
https://YOUR-USERNAME.github.io/YOUR-REPOSITORY/
```

The workflow uses the repository name automatically, so no code change is needed for the URL path.

## AI Usage
The original idea for this project was my own, inspired by friends from undergrad who are currently studying psychology. The concept was not generated by ChatGPT however ChatGPT was used to help develop and refine the project through an iterative process, reviewing its output and providing corrections, feedback, and direction. Its suggestions required revision to align with my intended design and goals. The current version reflects that ongoing process, with ChatGPT supporting development while I guided the project and made the final decisions.

### Prompts used
1. I would like to create a webpage where a 3d model of an individual neuron is displayed for psychology students to use during their studies to better visualize. When the user clicks on a specific part of the neuron they are able to learn about that parts functionality in relation to the whole neuron through a informational popup that does not block the model.
2. I would like this to have a different style, going away from the traditional modern look to a more cybernetic aesthetic. With this, the header should act more like a HUD rather than a navigation bar and also change the informational panel to a label popup with a line connecting from the selected part to the informational popup. If the area is clicked, the panel should remain but if the area is only hovered over for over 1.5 seconds if will display until the cursor is moved. I also need a readme.md file explaining how someone with no github knowledge can run this site.
3. Please remove the header portion of the site and condense the structure index from the bottom of the site into a button-activated popup located in the top right corner. You can keep “neuron_01” in the top lefthand corner but make it smaller and use a different font while maintaining the cybernetic aesthetic. There should not be any sectioning with a header or a footer, just the 3d model interactive portion and a HUD like name and menu-like structure index button.
4. The layout of this site is much better and more visually appealing! The font for “neuron_01” is also more pleasant and I would like for the entire site to reflect these stylistic changes. As for the model interaction, hovering should be the primary method for selecting a portion of the neuron. For instance, if the user has clicked on the nucleus but begins hovering on the dendrites the informational should change to the hover action of the dendrites. It is also okay if the informational popup covers some of the model but it should popup relatively outwards from the model. I would also like for the model itself to look more hyperrealistic.
5. The site is looking great so far and has correctly implemented most features. For the informational popup, could you make it slightly smaller and appear in any direction outwards of the model. If you must define a max size the model can be when the user zooms in order to accommodate for the popups, adjust it appropriately and show me the code that controls this behavior.
6. Could you have both the model and the informational popups be relative to each other in size. If the user were to zoom into the model while having a popup pinned, the popup should become smaller to accommodate and the model can expand freely. Remove the cap on zoom size. Also can you adjust the hover time needed for information to appear to be shorter and show me the section of code responsible for this action.
7. Could you adjust the models zoom function to be centered on the cursor, allowing the user to zoom into specific features. Also reimplement the max zoom but adjust it appropriately where the user can see details without losing any information or functionality.

## Reflection
